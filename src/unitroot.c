#include "unitroot.h"

/*

void ur_df(double *y, int N,const char* type, int lags, const char *selectlags) {
    int lag;
    if (lags < 0) {
        printf("Lags must be >= 0 \n");
        exit(-1);
    }

    lag = lags;
    lags++;
    
}
*/